# NEXA ERP (Pharma) Cloud

Cloud reference implementation for the same pharmacy ERP product.

Run:

```bash
npm install
npm start
```

Production deployment should use PostgreSQL/managed database, HTTPS, secure authentication, tenant isolation, backups and secrets management. The included JSON store is intentionally a development reference, not a production pharmacy database.
